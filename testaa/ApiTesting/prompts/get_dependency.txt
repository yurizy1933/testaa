请根据测试用例的需求帮我梳理接口文档，并且判断执行这个接口测试用例，需要依赖的接口和执行顺序。

测试用例信息：
- 用例 ID: {{case_id}}
- 接口名称：{{api_name}}
- 前置条件：{{precondition}}
- 测试点：{{testpoint}}
- 预期结果：{{expectation}}

要求：
1、请严格按照以下 json 格式生成，其中 case_id 为用例 ID，run_list 为执行接口测试用例所需的所有接口，run_num 为执行顺序，api_name 为执行接口的中文名，api_url 为实际执行接口的 url，method 为 HTTP 方法。

例如：
{
  "case_id": 1,
  "run_list": [
    {
      "run_num": 1,
      "api_name": "登录接口",
      "api_url": "/api/v1/login",
      "method": "POST",
      "request_body": {"username": "test", "password": "123456"},
      "description": "获取会话 token"
    },
    {
      "run_num": 2,
      "api_name": "获取学员练习评分记录接口",
      "api_url": "/api/v1/practice/score",
      "method": "GET",
      "query_params": {"c_s_id": "12345"},
      "description": "主接口测试"
    }
  ]
}

注意：
1、run_list 必须包含所有需要依赖的接口，包括主接口本身
2、api_url 必须是完整的接口路径，不包含域名
3、method 必须是标准的 HTTP 方法（GET/POST/PUT/DELETE 等）
4、如果前置条件中提到了"已登录"，则需要先调用登录接口
5、根据前置条件和测试点，智能推断每个接口需要的请求参数
6、只需要json个的内容，不需要其他任何描述
